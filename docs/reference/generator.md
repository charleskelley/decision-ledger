# `generator`

Synthetic event generator. Produces deterministic event streams for
the eight named ATO scenarios; calibration is informed by published
RBA dataset statistics (DR-10). Used by the eval harness's
`PipelineDriver`, by the integration smoke test, and by the
`generator run` CLI for manual exploration.

::: generator
    options:
      show_root_heading: false
